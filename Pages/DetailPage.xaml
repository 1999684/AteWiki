<Page x:Class="AtelierWiki.Pages.GameDetailPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="600" d:DesignWidth="800"
      Title="Game Detail">

    <Page.Resources>
        <!-- 颜色资源 -->
        <SolidColorBrush x:Key="TechBgBrush" Color="#0F172A"/>
        <SolidColorBrush x:Key="TechPanelBrush" Color="#1E293B"/>
        <SolidColorBrush x:Key="TechAccentBrush" Color="#00F0FF"/>
        <SolidColorBrush x:Key="TechTextBrush" Color="#E2E8F0"/>
        <SolidColorBrush x:Key="TechDimTextBrush" Color="#94A3B8"/>

        <!-- 自定义滚动条样式 -->
        <Style x:Key="TechScrollBarStyle" TargetType="{x:Type ScrollBar}">
            <Setter Property="Stylus.IsFlicksEnabled" Value="False"/>
            <Setter Property="Foreground" Value="{StaticResource TechPanelBrush}"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ScrollBar}">
                        <Grid x:Name="GridRoot" Width="{TemplateBinding Width}" Height="{TemplateBinding Height}" Background="{TemplateBinding Background}">
                            <Track x:Name="PART_Track" IsDirectionReversed="true" Focusable="false">
                                <Track.Thumb>
                                    <Thumb x:Name="Thumb">
                                        <Thumb.Template>
                                            <ControlTemplate TargetType="{x:Type Thumb}">
                                                <Border x:Name="ThumbBorder" Background="#3300F0FF" CornerRadius="4" Margin="1"/>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter TargetName="ThumbBorder" Property="Background" Value="{StaticResource TechAccentBrush}"/>
                                                        <Setter TargetName="ThumbBorder" Property="Effect">
                                                            <Setter.Value>
                                                                <DropShadowEffect Color="#00F0FF" BlurRadius="6" ShadowDepth="0"/>
                                                            </Setter.Value>
                                                        </Setter>
                                                    </Trigger>
                                                    <Trigger Property="IsDragging" Value="True">
                                                        <Setter TargetName="ThumbBorder" Property="Background" Value="#CC00F0FF"/>
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Thumb.Template>
                                    </Thumb>
                                </Track.Thumb>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="Orientation" Value="Horizontal">
                    <Setter Property="Height" Value="8"/>
                    <Setter Property="Width" Value="Auto"/>
                </Trigger>
                <Trigger Property="Orientation" Value="Vertical">
                    <Setter Property="Width" Value="8"/>
                    <!-- 修改为8以便能看到滚动条 -->
                    <Setter Property="Height" Value="Auto"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- 自定义按钮样式 -->
        <Style x:Key="TechButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#8899A6"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Grid Background="Transparent">
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <TextBlock x:Name="Arrow" Text="◄" FontSize="12" Margin="0,1,8,0" RenderTransformOrigin="0.5,0.5" VerticalAlignment="Center">
                                    <TextBlock.RenderTransform>
                                        <TranslateTransform X="0"/>
                                    </TextBlock.RenderTransform>
                                </TextBlock>
                                <TextBlock x:Name="Txt" Text="{TemplateBinding Content}" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Trigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="Arrow" Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.X)" To="-4" Duration="0:0:0.2" DecelerationRatio="0.5"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.EnterActions>
                                <Trigger.ExitActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="Arrow" Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.X)" To="0" Duration="0:0:0.2" DecelerationRatio="0.5"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.ExitActions>
                                <Setter Property="Foreground" Value="{StaticResource TechAccentBrush}"/>
                                <Setter Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect Color="#00F0FF" BlurRadius="8" ShadowDepth="0" Opacity="0.6"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <Grid Background="{StaticResource TechBgBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 1. 顶部标题栏 -->
        <Border Grid.Row="0" Padding="20,25,20,10">
            <StackPanel Orientation="Horizontal">
                <Button Content="BACK" Click="BtnBack_Click" Style="{StaticResource TechButtonStyle}" Margin="0,0,25,0"/>
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="DATA" Foreground="{StaticResource TechAccentBrush}" FontSize="12" Opacity="0.8" FontFamily="Consolas"/>
                    <TextBlock Text="{Binding PageTitle}" FontSize="22" FontWeight="Bold" Foreground="{StaticResource TechTextBrush}" Margin="0,-2,0,0">
                        <TextBlock.Effect>
                            <DropShadowEffect Color="#00F0FF" BlurRadius="10" ShadowDepth="0" Opacity="0.25"/>
                        </TextBlock.Effect>
                    </TextBlock>
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- 2. Tab 切换 -->
        <ListBox Grid.Row="1" 
                 ItemsSource="{Binding SheetNames}" 
                 SelectedItem="{Binding SelectedSheetName}"
                 Background="Transparent" BorderThickness="0" Margin="20,0,20,0">
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Horizontal"/>
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="Cursor" Value="Hand"/>
                    <Setter Property="Margin" Value="0,0,10,0"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListBoxItem">
                                <Grid Background="Transparent">
                                    <Border x:Name="ActiveBar" VerticalAlignment="Bottom" Height="2" Background="{StaticResource TechAccentBrush}" Visibility="Collapsed">
                                        <Border.Effect>
                                            <DropShadowEffect Color="#00F0FF" BlurRadius="4" ShadowDepth="0"/>
                                        </Border.Effect>
                                    </Border>
                                    <Border x:Name="Bd" Padding="15,8" Background="Transparent">
                                        <TextBlock x:Name="Txt" Text="{Binding}" Foreground="{StaticResource TechDimTextBrush}" FontWeight="SemiBold"/>
                                    </Border>
                                </Grid>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter TargetName="ActiveBar" Property="Visibility" Value="Visible"/>
                                        <Setter TargetName="Txt" Property="Foreground" Value="{StaticResource TechAccentBrush}"/>
                                        <Setter TargetName="Bd" Property="Background" Value="#1100F0FF"/>
                                    </Trigger>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="Txt" Property="Foreground" Value="White"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </ListBox.ItemContainerStyle>
        </ListBox>

        <Border Grid.Row="1" VerticalAlignment="Bottom" Height="1" Background="#33475D6F" Margin="20,0" Panel.ZIndex="-1"/>

        <!-- 3. 搜索框 -->
        <Border Grid.Row="2" Padding="20,15" Background="Transparent">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="350"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="关键字 >" FontFamily="Consolas" VerticalAlignment="Center" Margin="0,0,15,0" Foreground="{StaticResource TechAccentBrush}" FontSize="14"/>
                <Border Grid.Column="1" BorderBrush="#33475D6F" BorderThickness="1" CornerRadius="2" Background="#0B0E14">
                    <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" 
                             Padding="8" Background="Transparent" BorderThickness="0" 
                             Foreground="White" CaretBrush="{StaticResource TechAccentBrush}"
                             VerticalContentAlignment="Center" FontSize="14"/>
                </Border>
            </Grid>
        </Border>

        <!-- 4. 数据表格 (DataGrid) -->
        <DataGrid Grid.Row="3" 
                  x:Name="MyDataGrid"
                  ItemsSource="{Binding CurrentDataTable}"
                  SelectedItem="{Binding SelectedRow}"
                  SelectionMode="Single"
                  AutoGenerateColumns="True"
                  CanUserAddRows="False"
                  IsReadOnly="True"
                  
                  HeadersVisibility="Column"
                  GridLinesVisibility="Horizontal"
                  HorizontalGridLinesBrush="#33475D6F"
                  VerticalGridLinesBrush="Transparent"
                  
                  Background="Transparent"
                  BorderThickness="0"
                  RowBackground="Transparent"
                  AlternatingRowBackground="#05FFFFFF"
                  Foreground="{StaticResource TechTextBrush}"
                  Margin="20,0,20,20"
                  
                  ScrollViewer.CanContentScroll="True"
                  VerticalScrollBarVisibility="Auto" 
                  HorizontalScrollBarVisibility="Auto"
                  
                  MinColumnWidth="120">

            <DataGrid.Resources>
                <Style TargetType="{x:Type ScrollBar}" BasedOn="{StaticResource TechScrollBarStyle}"/>
            </DataGrid.Resources>

            <DataGrid.ColumnHeaderStyle>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Padding" Value="15,12"/>
                    <Setter Property="Background" Value="#1E293B"/>
                    <Setter Property="Foreground" Value="{StaticResource TechAccentBrush}"/>
                    <Setter Property="FontWeight" Value="Bold"/>
                    <Setter Property="BorderThickness" Value="0,0,0,2"/>
                    <Setter Property="BorderBrush" Value="#4400F0FF"/>
                    <Setter Property="HorizontalContentAlignment" Value="Left"/>
                </Style>
            </DataGrid.ColumnHeaderStyle>

            <DataGrid.RowStyle>
                <Style TargetType="DataGridRow">
                    <Setter Property="MinHeight" Value="40"/>
                    <Setter Property="Height" Value="Auto"/>
                    <Style.Triggers>
                        <!-- 鼠标悬停样式 -->
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="#2200F0FF"/>
                            <Setter Property="BorderBrush" Value="{StaticResource TechAccentBrush}"/>
                        </Trigger>

                        <Trigger Property="IsSelected" Value="True">
                            <!-- 背景：半透明亮黄 -->
                            <Setter Property="Background" Value="#88FFD700"/>
                            <!-- 边框：实心黄 -->
                            <Setter Property="BorderBrush" Value="white"/>
                            <!-- 字体：加粗白色，确保清晰 -->
                            <Setter Property="Foreground" Value="#FFFF00"/>
                            <Setter Property="FontWeight" Value="Bold"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.RowStyle>

            <DataGrid.CellStyle>
                <Style TargetType="DataGridCell">
                    <Setter Property="Padding" Value="10,8"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="DataGridCell">
                                <Border Padding="{TemplateBinding Padding}" Background="Transparent" BorderThickness="0">
                                    <ContentPresenter VerticalAlignment="Center" />
                                </Border>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                    <Style.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <!-- 背景：半透明亮黄 -->
                            <Setter Property="Background" Value="#88FFD700"/>
                            <!-- 边框：实心黄 -->
                            <Setter Property="BorderBrush" Value="white"/>
                            <!-- 字体：加粗白色，确保清晰 -->
                            <Setter Property="Foreground" Value="#FFFF00"/>
                            <Setter Property="FontWeight" Value="Bold"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.CellStyle>
        </DataGrid>


    </Grid>
</Page>
